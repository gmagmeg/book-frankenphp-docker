これは認証が必要なプライベートファイルのサンプルです。

このファイルは public/ ディレクトリの外に配置されているため、
直接URLからアクセスすることはできません。

X-Sendfile（X-Accel-Redirect）を使用することで、
PHP側でアクセス権を確認した後、FrankenPHPが直接ファイルを配信します。
PHPのメモリを介さないため、大容量ファイルでも効率的に動作します。
